<%= form_for(@book_a_lawn) do |f| %>
  <% if @book_a_lawn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_a_lawn.errors.count, "error") %> prohibited this book_a_lawn from being saved:</h2>

      <ul>
      <% @book_a_lawn.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
 <div class="field">
    <%= f.label :description %><br>
    
  <%= f.select :description, options_for_select([["Lawn 1"], ["Lawn 2"], ["Lawn 3"]]) %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
